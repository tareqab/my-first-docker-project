# My Docker Project
This project contains a simple Bash script running inside an Alpine Linux container.

## How to run
1. Build: `docker build -t my-script-image .`
2. Run: `docker run my-script-image`
